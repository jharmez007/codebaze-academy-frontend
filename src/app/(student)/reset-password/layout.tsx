export const metadata = {
  title: "Set a new password",
  description: "Set a new password for your student account.",
};

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}